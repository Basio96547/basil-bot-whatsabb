# خطة نظام موحّد للتحقق من الرقم (OTP) وإشعارات واتساب — لمشروعين

> خدمة واحدة خفيفة تُرسل رسائل تحقق (OTP) وإشعارات واتساب تلقائية، يستخدمها متجر Talisham والمشروع الثاني معاً، مع تمييز كل طلب حسب مصدره (المشروع/الموقع).

---

## 1. ما فحصته قبل كتابة الخطة

| المكان | الحالة الفعلية |
|---|---|
| `sms api` (هذا المجلد) | فارغ تماماً — لا يوجد كود بعد، هذا أول ملف فيه |
| متجر Talisham (`متجر اكتروني/store`، Astro على Cloudflare Workers + D1) | يعمل فعلياً على `talisham.com`. واتساب حالياً **رابط wa.me يدوي فقط** (`src/lib/whatsapp.ts`) — يفتح واتساب للموظف مع رسالة جاهزة ليضغط إرسال بنفسه، بدون أتمتة أو حساب API. لا يوجد OTP مطبَّق بعد. جدول `notifications_log` في قاعدة البيانات مُجهَّز مسبقاً (channel: whatsapp/sms/email + event + status) لكنه غير مُستخدم فعلياً حالياً. |
| المشروع الثاني | بحثت بكامل مجلد `متجر اكتروني` (الجذر + `store/src` + `store/worker` + `store/scripts`) ولم ألقَ بوتاً ثانياً — فقط نفس رابط wa.me اليدوي المذكور أعلاه. **مؤجَّل حسب طلبك** — التصميم أدناه مبني بحيث إضافته لاحقاً مجرد سطر إعدادات جديد (`project` آخر بجدول المشاريع، بند 4.1)، لا تغيير بالبنية. |

**خلاصة**: لا توجد "بوتات" جاهزة أستخرج منطقها — الموجود حالياً رابط يدوي فقط، فبنيّة الإرسال تُبنى من الصفر. لكن **بنية قاعدة بيانات المتجر (notifications_log) جاهزة ومتوافقة مع هذه الخطة**.

---

## 2. نطاق النظام

النظام المطلوب هو **رسائل معاملات (Transactional)** لعملاء حقيقيين لديهم علاقة فعلية بالمتجر/المشروع:
- كود تحقق (OTP) عند تسجيل حساب أو تأكيد رقم عند إتمام الطلب
- إشعارات حالة الطلب (استلام الطلب → التأكيد → خروج للتوصيل → التسليم) — نفس الجدول في خطة المتجر تحت "الرسائل التلقائية للعميل"

هذا **ليس** نظام إرسال جماعي/تسويقي لأرقام لا تربطها علاقة سابقة بالمتجر. هذا التمييز مهم تقنياً وليس شكلياً فقط: هو العامل الأكبر في تقليل احتمال حظر الرقم — رسالة تأكيد لعميل طلب فعلاً نمط طبيعي جداً بنظر واتساب، بعكس الإرسال الجماعي.

---

## 3. قرار معماري أساسي: ليش الخدمة مشروع منفصل عن الـ Workers

متجر Talisham يشتغل على Cloudflare Workers — بيئة serverless بلا حالة دائمة، ممتازة لصفحات الموقع لكنها لا تستضيف اتصال واتساب المستمر (Baileys يحتاج WebSocket مفتوح باستمرار مع سيرفرات واتساب + جلسة محفوظة بين الطلبات، وWorkers ما يدعم هذا النمط). فصل النظام بمشروع مستقل ("sms api") قرار صحيح تقنياً، والمتجر (وأي مشروع ثاني) يتواصلون معه عبر HTTP بسيط فقط.

---

## 4. مكوّنات النظام المقترح

```
   متجر Talisham   ──┐
                      ├──►  POST /notify  ──►  [ الخدمة الموحّدة ]
   المشروع الثاني   ──┘    (API key لكل مشروع)        │
                                                        ├─ طابور الإرسال (queue + retry)
                                                        ├─ محرك واتساب (Baileys)
                                                        ├─ محرك OTP/SMS
                                                        └─ سجل يوازي notifications_log
```

### 4.1 نقطة الدخول الموحّدة
- خدمة Node.js/TypeScript خفيفة واحدة، endpoint رئيسي: `POST /notify`
- كل طلب يحمل: `project` (`store` أو اسم المشروع الثاني)، `event` (`otp`, `order_created`, `order_confirmed`, `out_for_delivery`, `delivered`...)، `to`، وبيانات الرسالة. حقل `channel` **اختياري فقط** لتحديد يدوي عند الحاجة — افتراضياً الخدمة تقرر القناة تلقائياً حسب وجود واتساب على الرقم (بند 4.6)
- لكل مشروع **مفتاح API خاص** + قالب رسائله + هويته الخاصة — بهذا الشكل الخدمة تعرف "حسب العميل داخل من وين" بمجرد قراءة حقل `project`، دون خلط بين المشروعين

### 4.2 محرك واتساب
- **Baileys** (مكتبة مفتوحة المصدر لبروتوكول WhatsApp Web متعدد الأجهزة) — الخيار الواقعي الوحيد بدون حساب WhatsApp Business API الرسمي (يتطلب توثيق شركة عبر Meta ووسيط معتمد BSP، وعادة وسيلة دفع دولية)
- ربط لمرة واحدة بمسح QR من رقم واتساب فعلي (يمكن نفس الرقم المخزّن حالياً في إعداد `whatsapp_number` بالمتجر)، وبعدها الجلسة تبقى محفوظة على السيرفر دون حاجة لبقاء الهاتف متصلاً باستمرار

### 4.3 محرك OTP
قرار مؤكد (كلا القناتين، واتساب افتراضياً + SMS لاحقاً) — التفاصيل ببند 6.

**فترة تهدئة بين الطلبات (Resend Cooldown): 10 دقائق لكل رقم**
- بعد إرسال كود لرقم معيّن، نفس الرقم ما يقدر يطلب كوداً جديداً قبل مرور 10 دقائق — أي محاولة أبكر تُرفض بدون إرسال فعلي، والرد يتضمن الوقت المتبقي (مثلاً "انتظر 6 دقائق ثم حاول مجدداً") حتى تقدر الواجهة تعرضه للعميل بوضوح
- الفحص بجدول واحد بسيط: `last_sent_at` لكل رقم — طلب جديد يُقارَن بالوقت الحالي قبل أي محاولة إرسال
- هذا **منفصل عن صلاحية الكود نفسه** (عادةً أقصر، ٥ دقائق مثلاً): الكود المُرسَل يبقى صالحاً للتحقق خلال مدته الخاصة، والتهدئة فقط تمنع طلب كود إضافي جديد قبل انتهاء المدة
- نفس الآلية تحل غرضين معاً: تمنع إغراق رقم عميل برسائل متكررة (OTP bombing، مذكور ببند 11) **و** تقلل حِمل الإرسال على رقم واتساب المرسِل نفسه (نقطة إضافية لتخفيف الحظر، بند 5)

**حماية التحقق نفسه من التخمين (كان ناقصاً، أساسي لا اختياري)**
- كود 6 أرقام + **حد أقصى 5 محاولات تحقق خاطئة** لكل كود — بعد تجاوزها يُلغى الكود فوراً ويحتاج العميل طلب كود جديد (وينطبق عليه تهدئة الـ 10 دقائق كالمعتاد). بدون هذا الحد، كود مكوّن من أرقام قليلة قابل للتخمين الآلي السريع خلال مدة صلاحيته
- الكود يُخزَّن **مُجزَّأً (hash) لا نصاً صريحاً** بقاعدة البيانات، بنفس منطق كلمات المرور — لأنه لو أحد وصل لقاعدة البيانات مباشرة (نسخة احتياطية مسروقة مثلاً) ما يقدر يقرأ الأكواد الصالحة حالياً ويستخدمها

### 4.4 الطابور والسجل
- طابور بسيط (SQLite كافٍ لحجم الطلبات المتوقع) يرسل برتم بطيء ومتحكم به، مع إعادة محاولة عند الفشل
- تسجيل كل رسالة بنفس فكرة `notifications_log` الموجودة بالمتجر (القناة، الحدث، المستلم، الحالة)، مع endpoint ثانٍ: `GET /status/:id` يسحب منه المتجر حالة أي رسالة

### 4.5 قوالب الرسائل ونظام التنويع
كل حدث (`event`) له **3–5 صيغ نصية بنفس المعنى**، مو نص ثابت واحد — الخدمة تختار صيغة عشوائياً بكل إرسال (لا بالتتابع الثابت):
- التنويع بالصياغة وترتيب العناصر ووجود/غياب الإيموجي وعلامات الترقيم — **لا** بالمعنى أو الأرقام: رقم الطلب والمبلغ يبقيان دقيقين حرفياً بكل الصيغ دائماً
- مثال لحدث "تأكيد الطلب" (3 صيغ فقط للتوضيح، الفعلي يحتاج أكثر):
  - "تم تأكيد طلبك رقم #{order}، جاري التجهيز الآن 👍"
  - "طلبك #{order} مؤكد ✅ وراح نبدأ التجهيز"
  - "أخبار حلوة {name}، طلبك #{order} تأكد وبنجهزه"
- **السبب**: نص حرفي متطابق 100% مُرسَل لمئات الأرقام المختلفة خلال فترة قصيرة من أوضح الأنماط التي تكشفها أنظمة واتساب كسلوك بوت — بغض النظر عن صحة المحتوى أو كون المستلمين عملاء حقيقيين
- كل رسالة تُسجَّل مع رقم الصيغة المستخدمة (`template_variant`) بجدول السجل، يفيد لاحقاً لو احتجت تتبع مشكلة بصيغة معينة

### 4.6 التوجيه التلقائي بين واتساب وSMS
قبل أي إرسال، الخدمة تتحقق أولاً: هل هذا الرقم مسجّل على واتساب أصلاً؟ (Baileys يدعم هذا الفحص مباشرة — `onWhatsApp()` — بدون إرسال أي رسالة، فقط سؤال سيرفرات واتساب عن حالة الرقم)
- **الرقم عنده واتساب** → إرسال عبر واتساب مباشرة، بلا حاجة لـ SMS إطلاقاً
- **الرقم ما عنده واتساب** → إرسال عبر SMS مباشرة (بمجرد ربط مزوّد، بند 6) — بلا محاولة واتساب فاشلة تضيّع وقتاً وتترك أثراً بلا فائدة
- نتيجة الفحص تُخزَّن مؤقتاً (Cache) لكل رقم لمدة أسبوع تقريباً — عميل يكرر التعامل (تأكيد، تسليم...) خلال نفس المدة ما يحتاج فحصاً جديداً، يقلل عدد الاستعلامات لسيرفرات واتساب
- لو الفحص نفسه فشل (مشكلة شبكة مؤقتة) → رجوع للسلوك الاحتياطي: محاولة واتساب أولاً، وإذا فشل الإرسال فعلياً نزول لـ SMS
- **يُذكر بوضوح**: لحد ما يتوفر مزوّد SMS فعلي (بند 6 لسا مفتوح)، أي رقم بدون واتساب حالياً **ما فيه طريقة توصله أبداً** — سبب إضافي يرجّح تحديد مزوّد SMS بسرعة، مو تفصيلاً بلا ضغط

---

## 5. الواقعية حول "مضاد الحظر" — لا يوجد ضمان مطلق

أي رقم يرسل رسائل آلية بدون WhatsApp Business API الرسمي معرّض لخطر الحظر — هذا صحيح لأي أداة تدّعي "anti-ban" بلا استثناء، ولا أحد يقدر يضمن عكس ذلك. اللي فعلاً يقلل الخطر (لا يلغيه):

1. **الإرسال فقط لعملاء حقيقيين طلبوا فعلاً** — لا أرقام عشوائية أو قوائم مجمّعة. أهم نقطة على الإطلاق
2. **تنويع نص الرسائل** (تفصيل كامل ببند 4.5) — رسالة متطابقة حرفياً لمئات الأرقام أوضح إشارة بوت بالنسبة لواتساب، حتى لو المحتوى مشروع 100%
3. **تحكم صارم بالمعدّل**: عدد رسائل محدود بالدقيقة، مع تأخير عشوائي بينها يحاكي نمط الإنسان
4. **تهيئة تدريجية لأي رقم جديد**: حجم قليل بالبداية يزيد تدريجياً على أسابيع
5. **رقمان احتياط جاهزان** + رجوع تلقائي لرابط wa.me اليدوي (الموجود حالياً) عند حظر الرقم الأساسي — بذا لا ينقطع التواصل مع العملاء أبداً
6. مراقبة مؤشرات الإنذار (فشل تكرار الاتصال، رفض الرسائل) ووقف تلقائي مؤقت عند ظهورها

**نصيحة صريحة**: لو كبر حجم الطلبات لدرجة يصبح الاعتماد الكامل على رقم غير رسمي مخاطرة حقيقية على العمل، التسجيل بـ WhatsApp Business API الرسمي (عبر وسيط BSP) يستاهل إعادة النظر رغم التكلفة، لأنه الخيار الوحيد بلا مخاطرة حظر من الأساس.

---

## 6. قرار مؤكد: OTP عبر القناتين معاً

اخترت الاثنين. التصميم: محرك OTP مستقل عن القناة — يولّد الكود ويخزّنه ويتحقق منه بغض النظر عن طريقة التسليم، وطريقة التسليم نفسها قابلة للتبديل لكل طلب:
- **افتراضياً عبر واتساب** (محرك Baileys نفسه) — لأنه لا يحتاج مزود خارجي ويشتغل من أول يوم
- **SMS كخيار إضافي بمجرد ما تحدد مزوّداً فعلياً** — يحتاج تخبرني أي مزود متاح لك وله API تقدر تدفع له، عندها يُضاف كطبقة توصيل ثانية بدون تغيير منطق توليد/تحقق الكود

التوجيه بين القناتين لكل رسالة (مو بس OTP) مبني على فحص وجود واتساب أولاً، لا على تجربة فاشلة ثم تراجع — التفصيل الكامل ببند 4.6.

---

## 7. قرار مؤكد: الجوال عبر Termux

اخترت الجوال. مقبول تقنياً، لكن حتى تصير موثوقيته قريبة من سيرفر حقيقي لازم هالإعدادات تحديداً (مو اختيارية):

1. **`termux-wake-lock`**: يمنع أندرويد يوقف Termux لما الشاشة تنطفي — بدونه العملية تتجمد بعد دقائق من عدم الاستخدام
2. **استثناء Termux من تحسين البطارية** (إعدادات النظام → البطاريه → Termux → بلا قيود) — وإلا أندرويد يقتل العملية بالخلفية بعد فترة رغم wake-lock
3. **Termux:Boot** (تطبيق منفصل من نفس مطوّري Termux، متوفر F-Droid) لإعادة تشغيل الخدمة تلقائياً بعد أي إعادة تشغيل للهاتف أو انقطاع كهرباء
4. **شاحن دائم + واي فاي مستقر** — الهاتف عملياً سيرفر الآن، لازم يبقى موصول بالطاقة دائماً
5. **مراقبة خارجية**: لأن الجوال أقل ثباتاً من VPS، الخدمة تحتاج health-check دوري (حتى لو بسيط: ping كل ٥ دقائق) يرسل لك تنبيه واتساب/بريد لو الخدمة توقفت — بدون هذا ممكن تنقطع الرسائل أياماً بدون ما تلاحظ

هذا الإعداد يخليه يشتغل، لكن يبقى أقل ثباتاً من VPS بـ5$/شهر (لا انقطاع كهرباء، لا قتل عمليات، لا اعتماد على شبكة موبايل). خيار الانتقال لـ VPS يبقى مفتوح لاحقاً بدون أي تغيير بالكود — فقط نقل نفس الخدمة لسيرفر آخر.

---

## 8. الاستمرارية والتعافي التلقائي عند انقطاع النت

"يرجع يشتغل تلقائياً ودائماً" يحتاج تعامل مع الانقطاع على ستة مستويات منفصلة — كل مستوى يغطي عطلاً لا يغطيه غيره، وتجاهل أي واحد منها يعني انقطاع فعلي رغم وجود الباقي:

**1. انقطاع الاتصال بواتساب نفسه (الأكثر تكراراً)**
Baileys يصدر حدث `connection.update` عند أي فصل. الكود يفحص سبب الفصل:
- أي سبب غير "تسجيل خروج فعلي" (`DisconnectReason.loggedOut`) → إعادة اتصال تلقائية فورية بنفس الجلسة المحفوظة، بدون مسح QR من جديد. هذا يغطي انقطاع الواي فاي، تبديل الشبكة، أعطال واتساب المؤقتة
- تسجيل خروج فعلي (401 — نادر: يصير لو الجلسة سُحبت من الهاتف يدوياً أو من رقم آخر) هو الوحيد اللي يحتاج تدخلك (QR جديد) → تنبيه فوري لك بهذا الفرق تحديداً حتى ما يمر وقت طويل بدون ما تنتبه
- محاولات الاتصال بفواصل متزايدة (٥ث، ١٥ث، دقيقة...) لا محاولات متلاحقة بلا حدود

**2. انهيار العملية نفسها (Node process يموت أو أندرويد يقتله)**
منطق إعادة الاتصال جوّه الكود عديم الفائدة لو العملية ماتت بالكامل. يحتاج مُشرِف خارجي يرجّعها:
- `pm2` داخل Termux (يشتغل بلا مشاكل، ويوفر سجلات + `pm2 resurrect` بعد أي إعادة تشغيل) — أو بديل أبسط: حلقة شل لا نهائية (`while true; do node index.js; sleep 3; done`)

**3. إعادة تشغيل الهاتف أو انقطاع الكهرباء الكامل**
يغطيه `Termux:Boot` (بند 7) — بمجرد ما الهاتف يقلع من جديد، السكربت يشتغل تلقائياً بدون أي لمسة يدوية.

**4. الرسائل نفسها ما تُفقد أثناء أي انقطاع من الثلاثة فوق**
الأهم عملياً: كل رسالة تُكتب بجدول `pending` (بند 4.4) **قبل** محاولة الإرسال لا بعدها. حلقة الإرسال تسحب من هذا الجدول باستمرار وتحدّث حالتها بعد كل محاولة. فلو انقطع أي شيء أثناء رسالة قيد المعالجة، تبقى `pending` بالقاعدة، وأول ما ترجع العملية (بأي مستوى من الثلاثة فوق) تكمل من نفس الجدول تلقائياً — بلا كود "تعافي" خاص، لأن الطابور هو مصدر الحقيقة لا ذاكرة العملية.

**5. مراقبة خارجية (تكتشف العطل الصامت)**
مين يراقب أن كل هذا فعلاً يشتغل؟ `GET /health` بالخدمة يرجّع حالة اتصال واتساب + عدد الرسائل المتراكمة بالطابور. جهة خارجية تفحصها دورياً — حتى ووركر المتجر نفسه، عبر نفس آلية الـ Cron الموجودة أصلاً لسلال المتروكة (`worker/entry.ts`) يقدر يستدعيها كل فترة ويرسل لك تنبيه لو ما ردت أو رجعت حالة سيئة. بدون هذي الطبقة، لو الهاتف انطفى فعلياً ما فيه شيء بالمستويات فوق يصلح نفسه، ولازم تعرف بسرعة لا بعد أيام.

**6. فقدان جلسة واتساب نفسها (تلف تخزين، حذف بالخطأ)**
كل الطبقات فوق تفترض ملفات جلسة Baileys (بيانات الدخول) موجودة وسليمة على الهاتف — لو انحذفت أو تلفت (مسح بيانات Termux بالغلط، تلف تخزين)، كل الطبقات السابقة عاجزة والحل الوحيد وقتها QR جديد من الصفر. لذلك: **نسخة احتياطية مشفّرة** لمجلد جلسة Baileys تُرفع تلقائياً على Cloudflare R2 (متوفر أصلاً بحساب المتجر — بحاوية/مسار منفصل عن صور المنتجات العامة، لا نفس الحاوية `MEDIA`) في كل مرة تتغيّر فيها بيانات الجلسة (حدث `creds.update` من Baileys نفسه، لا جدول زمني ثابت). الرفع مشفّر بمفتاح تحتفظ به أنت فقط — حتى لو تسرّب الوصول لحساب R2 وحده ما يكفي لاستخدام الجلسة. الاستعادة عند الحاجة: تحميل آخر نسخة، فك التشفير، إعادتها لمكانها، إعادة تشغيل الخدمة — بدون QR جديد.

**الحدود الواقعية**: هذا يغطي انقطاع النت، تجمد التطبيق، انهيار العملية، إعادة تشغيل الهاتف، وفقدان ملفات الجلسة — كل أعطال "مؤقتة" أو "قابلة للاستعادة". ما يغطيه: بطارية فرغت بالكامل بلا شاحن، أو عطل فعلي بهاردوير الهاتف. هذي فقط رقم/جهاز احتياط ثاني يحلها (بند 5، نقطة 4) — لا يوجد كود يصلح هاتفاً معطلاً.

---

## 9. الصمود تحت الضغط — منع الانهيار بسبب الحمل الزائد

مختلف عن الاستمرارية (بند 8): ذاك يتعامل مع الانقطاع (نت/عملية/إعادة تشغيل)، وهذا يمنع الانهيار أصلاً لما يجي حمل كبير دفعة وحدة (مثلاً عرض أو تخفيض يجيب عشرات الطلبات بنفس الدقيقة) — مهم بالذات لأن المضيف هاتف بموارد محدودة (بند 7)، لا سيرفر بموارد مرنة.

**1. فصل الاستقبال عن الإرسال (الأساس، موجود ببند 4.4)**
`POST /notify` لا يحاول الإرسال الفعلي إطلاقاً — يتحقق من شكل الطلب فقط (بدون أي اتصال خارجي) ويكتب سطراً بالطابور ويرد فوراً. لو وصلت 500 طلب بنفس الثانية، الخدمة تكتب 500 سطر بقاعدة بيانات محلية سريعة وترد — لا علاقة لهذا بسرعة واتساب أو الشبكة. حلقة الإرسال المنفصلة (worker loop) تسحب من الطابور بمعدلها المضبوط (بند 5) بغض النظر عن حجم الدفعة الواردة.

**2. سقف أقصى للطابور (Backpressure)**
لو تراكمت رسائل معلّقة أكثر من حد معقول (مثلاً 5000)، `/notify` يرفض طلبات جديدة مؤقتاً برد `429` بدل قبولها بلا حدود — يحمي تخزين وذاكرة الهاتف من الامتلاء بسبب خلل متكرر (مثلاً كود بالمتجر يعيد إرسال نفس الطلب بحلقة خاطئة لا نهائية).

**3. معالجة بدفعات صغيرة، لا تحميل الطابور كله بالذاكرة دفعة وحدة**
حلقة الإرسال تسحب مجموعة صغيرة فقط بكل مرة (٢٠ رسالة مثلاً)، تعالجها، ثم تسحب التالية — لا `SELECT *` على كامل الطابور مرة وحدة، حتى لو كبر حجمه لآلاف الرسائل.

**4. سقف ذاكرة + إعادة تشغيل استباقية**
`pm2` (بند 8) يدعم `max_memory_restart` — سقف مناسب لجوال (٢٠٠MB مثلاً)، وإذا تجاوزته العملية يعيدها `pm2` نظيفة تلقائياً **قبل** ما تنهار فعلياً بنفاد الذاكرة (OOM) بشكل غير منضبط.

**5. مهلة زمنية (timeout) على أي اتصال خارجي**
أي استدعاء لواتساب أو مزود SMS له مهلة قصوى (١٥ ثانية مثلاً) — بدونها اتصال عالق واحد يجمّد الدور كله خلفه ويتراكم الضغط بصمت.

**6. قاطع دائرة عند تكرار الفشل (Circuit breaker)**
لو صارت عدة محاولات إرسال متتالية فاشلة (مؤشر تعطّل مؤقت بواتساب أو المزوّد)، حلقة الإرسال توقف نفسها لفترة قصيرة بدل إعادة المحاولة بسرعة وبلا توقف — يمنع استهلاك بطارية/معالج الهاتف بحلقة فشل متكررة عديمة الفائدة.

**7. `/health` يعكس الضغط لا فقط "شغّال/متوقف"**
نفس نقطة الفحص من بند 8 تُوسَّع لترجع أيضاً حجم الطابور الحالي — لو كبر بشكل غير طبيعي (إشارة ضغط حتى قبل أي انهيار)، تنبيه مبكر بدل اكتشاف المشكلة بعد تراكمها أيام.

كل هذا يبقى خفيفاً عمداً (بلا أدوات إضافية معقّدة مثل Redis أو طوابير موزّعة) — الحماية كلها قواعد بسيطة فوق نفس قاعدة SQLite الموجودة أصلاً، مناسبة لحجم متجر لا لبنية مؤسسات ضخمة.

---

## 10. سياسة الاحتفاظ بالسجل وحذف البيانات القديمة

كل رسالة وكل رقم هاتف يبقى مخزّناً بالسجل إلى الأبد ما لم يُحدَّد خلاف ذلك — تراكم بلا داعٍ على تخزين محدود أصلاً (الهاتف، بند 7)، وزيادة غير ضرورية بحساسية البيانات لو انكشف الجهاز يوماً.

- **سجل الإشعارات العام** (`notifications_log` الموازي، بند 4.4): يُحذف تلقائياً بعد **90 يوماً** من تاريخ الإرسال — مدة كافية لمراجعة أي نزاع أو استفسار عميل ("وصلتني رسالة التأكيد؟")، وبعدها لا فائدة حقيقية من الاحتفاظ به
- **أكواد OTP نفسها** (المُجزَّأة أصلاً، بند 4.3): تُحذف أسرع بكثير — خلال ساعة من الاستخدام أو انتهاء الصلاحية، أياً كان أقرب. ما فيه سبب للاحتفاظ بكود منتهٍ حتى لو مُجزَّأً
- **جدول فحص وجود واتساب** (Cache، بند 4.6): يُحدَّث/يُستبدَل تلقائياً كل أسبوع أصلاً حسب تصميمه، لا يحتاج سياسة حذف منفصلة
- **آلية التنفيذ**: مهمة تنظيف بسيطة تشتغل مرة يومياً ضمن نفس عملية الخدمة (هي أصلاً دائمة التشغيل، بند 8) — حذف بشرط التاريخ، بلا حاجة لأي أداة خارجية
- المدد أعلاه افتراضية وقابلة للتعديل — لو احتجت الاحتفاظ لفترة أطول لأسباب محاسبية لاحقاً، أعلمني بالمدة المناسبة

---

## 11. الأمان
- مفتاح API منفصل لكل مشروع، قابل للتغيير بسهولة لو تسرّب
- الخدمة الموحّدة غير مكشوفة على الإنترنت العام بدون مصادقة — فقط المتجر والمشروع الثاني يستدعونها
- حدّ لمعدل الطلبات على endpoint الـ OTP: تهدئة 10 دقائق بين كل طلب وآخر لنفس الرقم (التفصيل الكامل ببند 4.3)، حتى لا يصير أداة لإغراق رقم عميل معيّن برسائل متكررة (OTP bombing) — نفس مبدأ الحماية المطبّق على تسجيل الدخول بالمتجر
- جلسة واتساب (ملفات Baileys) تُخزَّن بمكان محمي على السيرفر ومنسوخة احتياطياً مشفّرة (بند 8، نقطة 6) — لأنها تعادل مفتاح الدخول الكامل للرقم

---

## 12. مراحل التنفيذ المقترحة

| المرحلة | التسليم |
|---|---|
| 1. الأساس + الاستمرارية + الصمود تحت الضغط | خدمة Node واحدة، `POST /notify` (تحقق سريع + كتابة بالطابور فقط)، طابور SQLite بسقف أقصى، محرك Baileys مربوط برقم تجريبي، إعادة اتصال تلقائية، `pm2` بحد ذاكرة + `Termux:Boot`، نسخ احتياطي مشفّر للجلسة على R2، `GET /health` يعكس الحالة والضغط — كل هذا جزء من الأساس لا إضافات لاحقة |
| 2. ربط المتجر | استبدال منطق wa.me اليدوي تدريجياً — يبقى fallback، والإرسال التلقائي يصبح الأساس |
| 3. OTP | القناتان + تهدئة 10 دقائق + حد المحاولات حسب بند 6 و4.3 |
| 4. التوجيه والتنظيف | فحص وجود واتساب (بند 4.6) + مهمة حذف السجل القديم (بند 10) |
| 5. المشروع الثاني | إضافة `project` جديد بمجرد توفر مساره (مؤجَّل حالياً) |
| 6. المراقبة | تقرير يومي بسيط: عدد المُرسَل، الفاشل، حالة الجلسة وحجم الطابور |

---

*آخر تحديث: 2026-08-12*
