# سكربتات الصدفة تُنفَّذ على الجوال (Termux) لا على ويندوز. بلا هذا السطر
# يحوّلها git عند السحب على ويندوز إلى CRLF، فيصير سطر الشيبانغ
# `#!/data/data/com.termux/files/usr/bin/sh\r` — مسار غير موجود، والخطأ
# الذي يظهر هو "bad interpreter" لا "نهايات أسطر خاطئة".
*.sh text eol=lf
